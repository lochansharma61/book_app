
const baseApiUrl = 'https://www.googleapis.com'
const API_HOST = 'http://url'
apiVersion = 'api'

const apiUrls = {
    feedsList: `${baseApiUrl}/books/v1/volumes?filter=free-ebooks&q=a`,
}


export {
    apiUrls,
    API_HOST,
}